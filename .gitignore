# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit
.env
.env.*
# ...except the template, which holds variable NAMES only, no values.
!.env.example

# PWA dev artifacts
dev-dist
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Neon CLI link file — this project is on Supabase. Keeping this out stops
# `neon env pull` / `link` / `checkout` regenerating DATABASE_URL + NEON_*.
.neon

# Local plaintext credential scratch files — never commit.
Database password.txt
*password*.txt
*credentials*.txt
